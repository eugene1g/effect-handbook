{
  "schemaVersion": 1,
  "description": "Intent-retrieval regression cases for the Effect 4 capability catalog. Each case also acts as a primitive-selection rubric for periodic LLM evaluations.",
  "requiredCapabilityFields": ["errorChannel", "requirements", "lifetime"],
  "cases": [
    { "id": "async-program", "query": "model interruptible asynchronous work with typed failures and dependencies", "expected": ["effect.program"], "acceptable": ["effect.promise-boundary"], "forbidden": ["use async await throughout domain services"] },
    { "id": "promise-api", "query": "wrap a Promise API and map rejected promises to a typed error", "expected": ["effect.promise-boundary"], "acceptable": ["effect.program"], "forbidden": ["let rejection become an untyped defect"] },
    { "id": "cleanup-interrupt", "query": "close a resource reliably when the fiber is interrupted", "expected": ["effect.resource"], "acceptable": ["layer.scoped"], "forbidden": ["return the resource after its Scope closes"] },
    { "id": "owned-child", "query": "start background work that stops when its component shuts down", "expected": ["effect.fork-scoped"], "acceptable": [], "forbidden": ["forkDetach without owner-aware shutdown"] },
    { "id": "dependency-injection", "query": "define an injectable business service with a test substitute", "expected": ["context.service"], "acceptable": ["layer.service"], "forbidden": ["hide the service in a global singleton"] },
    { "id": "fiber-policy", "query": "provide a default fiber-local setting that callers may override", "expected": ["context.reference"], "acceptable": [], "forbidden": ["use a scoped resource service"] },
    { "id": "wire-services", "query": "compose service implementations into one dependency graph", "expected": ["layer.service"], "acceptable": ["context.service"], "forbidden": ["provide every dependency at every call site"] },
    { "id": "resource-service", "query": "provide a connection pool as an application-lifetime service", "expected": ["layer.scoped"], "acceptable": ["effect.resource"], "forbidden": ["discard the finalizer"] },
    { "id": "framework-runtime", "query": "reuse Effect services from event callbacks in a non Effect framework", "expected": ["managed-runtime.integration"], "acceptable": [], "forbidden": ["build a runtime for every callback"] },
    { "id": "optional-value", "query": "represent expected absence when no explanation is needed", "expected": ["option.optional"], "acceptable": [], "forbidden": ["invent an exception for absence"] },
    { "id": "pure-failure", "query": "return a pure synchronous success or typed failure value", "expected": ["result.expected-failure"], "acceptable": [], "forbidden": ["introduce Effect when there is no effect"] },
    { "id": "full-failure", "query": "inspect typed failures defects and interruption together", "expected": ["cause.diagnostics"], "acceptable": [], "forbidden": ["discard the Cause before diagnostics"] },
    { "id": "promise-cell", "query": "wait for one value that another fiber will complete exactly once", "expected": ["deferred.one-shot"], "acceptable": [], "forbidden": ["poll a Ref in a loop"] },
    { "id": "reusable-gate", "query": "pause many fibers behind a reusable open close gate", "expected": ["latch.gate"], "acceptable": ["deferred.one-shot"], "forbidden": ["use Deferred when the gate must close again"] },
    { "id": "worker-mailbox", "query": "bounded producer consumer mailbox with backpressure", "expected": ["queue.work"], "acceptable": ["stream.pipeline"], "forbidden": ["use an unbounded array as a queue"] },
    { "id": "broadcast", "query": "broadcast every new event to all active subscribers", "expected": ["pubsub.broadcast"], "acceptable": ["subscription-ref.state-stream"], "forbidden": ["use Queue when every subscriber must receive each value"] },
    { "id": "mutex", "query": "guard a critical section with a mutex or permit count", "expected": ["semaphore.limit"], "acceptable": [], "forbidden": ["implement locking with a boolean Ref"] },
    { "id": "parallel-array", "query": "process one finite array with at most eight operations in parallel", "expected": ["concurrency.bounded"], "acceptable": ["semaphore.limit"], "forbidden": ["use unbounded concurrency"] },
    { "id": "object-pool", "query": "reuse a bounded set of expensive acquired objects", "expected": ["pool.resources"], "acceptable": [], "forbidden": ["use a Semaphore when objects themselves need acquisition"] },
    { "id": "counter-state", "query": "atomically update one pure in memory counter", "expected": ["ref.atomic-state"], "acceptable": [], "forbidden": ["use STM for a single pure cell without composition"] },
    { "id": "effectful-state", "query": "serialize effectful updates to one shared state cell", "expected": ["synchronized-ref.effectful"], "acceptable": ["stm.transaction"], "forbidden": ["perform Effects inside Ref.update"] },
    { "id": "observable-state", "query": "read current state and stream all later changes", "expected": ["subscription-ref.state-stream"], "acceptable": ["pubsub.broadcast"], "forbidden": ["lose the current snapshot"] },
    { "id": "atomic-many", "query": "atomically change several in memory values and retry on a condition", "expected": ["stm.transaction"], "acceptable": [], "forbidden": ["call external HTTP inside a transaction"] },
    { "id": "large-sequence", "query": "incrementally process a large or infinite effectful sequence with backpressure", "expected": ["stream.pipeline"], "acceptable": [], "forbidden": ["collect the entire input before processing"] },
    { "id": "stream-consumer", "query": "build a reusable consumer that folds stream input and can leave leftovers", "expected": ["sink.consume"], "acceptable": [], "forbidden": ["drop to Channel for a simple fold"] },
    { "id": "stream-protocol", "query": "implement a low level typed bidirectional streaming protocol", "expected": ["channel.protocol"], "acceptable": [], "forbidden": ["use Channel for an ordinary map"] },
    { "id": "finite-sequence", "query": "transform a small finite pure collection already in memory", "expected": ["collection.finite"], "acceptable": [], "forbidden": ["introduce Stream without effects or incremental demand"] },
    { "id": "retry-backoff", "query": "retry only transient typed failures with exponential backoff", "expected": ["schedule.retry"], "acceptable": [], "forbidden": ["retry every defect and permanent error"] },
    { "id": "repeat-success", "query": "repeat a successful effect on a cadence", "expected": ["schedule.repeat"], "acceptable": [], "forbidden": ["use retry to repeat successes"] },
    { "id": "poll-condition", "query": "poll until a returned value satisfies a condition", "expected": ["schedule.poll"], "acceptable": ["schedule.repeat"], "forbidden": ["write a manual sleep loop"] },
    { "id": "cron-job", "query": "run an in process cron job on a calendar schedule", "expected": ["schedule.cron"], "acceptable": ["schedule.repeat"], "forbidden": ["claim an in process cron survives restart"] },
    { "id": "memoized-lookup", "query": "memoize an effectful lookup with TTL and deduplicate concurrent misses", "expected": ["cache.lookup"], "acceptable": ["request-resolver.batch"], "forbidden": ["assume refresh calls are deduplicated"] },
    { "id": "cached-resource", "query": "cache keyed scoped resources and finalize them on eviction", "expected": ["scoped-cache.resource"], "acceptable": ["pool.resources"], "forbidden": ["put live resources in an ordinary Cache"] },
    { "id": "background-refresh", "query": "serve the current value while refreshing it periodically in the background", "expected": ["resource.refreshable"], "acceptable": ["cache.lookup"], "forbidden": ["claim failed refresh discards the last good value"] },
    { "id": "data-loader", "query": "batch and deduplicate many keyed requests in one request window", "expected": ["request-resolver.batch"], "acceptable": ["sql.resolver"], "forbidden": ["issue one remote call per requested key"] },
    { "id": "validate-boundary", "query": "validate unknown JSON at an application boundary", "expected": ["schema.boundary"], "acceptable": [], "forbidden": ["cast unknown input to a domain type"] },
    { "id": "encoded-domain", "query": "transform a wire string into a validated domain value and encode it back", "expected": ["schema.transform"], "acceptable": ["schema.boundary"], "forbidden": ["confuse Schema Type with Encoded"] },
    { "id": "schema-class", "query": "define a schema backed domain class with generated constructor and codec", "expected": ["schema.class"], "acceptable": [], "forbidden": ["annotate away the concrete schema type"] },
    { "id": "domain-error", "query": "model a recoverable tagged domain error in the Effect error channel", "expected": ["errors.tagged"], "acceptable": [], "forbidden": ["throw a global Error for an expected failure"] },
    { "id": "outbound-http", "query": "make an outbound HTTP request with middleware retries and decoded response", "expected": ["http.client"], "acceptable": [], "forbidden": ["use HttpApi to call an untyped external endpoint"] },
    { "id": "low-level-route", "query": "route low level HTTP requests without defining a contract first", "expected": ["http.router"], "acceptable": ["http.api"], "forbidden": ["build an HttpApi when dynamic routing is the goal"] },
    { "id": "contract-http", "query": "share a typed HTTP contract between server client OpenAPI and middleware", "expected": ["http.api"], "acceptable": [], "forbidden": ["duplicate request schemas in router and client"] },
    { "id": "typed-rpc", "query": "invoke typed operations over a transport with streaming and middleware", "expected": ["rpc.contract"], "acceptable": ["http.api"], "forbidden": ["use RPC when public REST semantics are required"] },
    { "id": "sql-query", "query": "run parameterized SQL in a transaction and stream rows", "expected": ["sql.client"], "acceptable": [], "forbidden": ["interpolate untrusted values into SQL text"] },
    { "id": "sql-batching", "query": "batch database lookups by key to avoid N plus one queries", "expected": ["sql.resolver"], "acceptable": ["request-resolver.batch"], "forbidden": ["query once per row"] },
    { "id": "persist-exit", "query": "persist a schema encoded Effect result and load it after restart", "expected": ["persistence.store"], "acceptable": [], "forbidden": ["treat the store lookup as a pure value"] },
    { "id": "durable-cache", "query": "cache computed values across a process restart", "expected": ["persistence.cache"], "acceptable": ["persistence.store"], "forbidden": ["use an in memory Cache for restart durability"] },
    { "id": "long-running-process", "query": "resume a multi step business process after a crash", "expected": ["workflow.durable"], "acceptable": [], "forbidden": ["assume an ordinary Effect survives restart"] },
    { "id": "workflow-side-effect", "query": "perform an external side effect safely from a durable workflow", "expected": ["workflow.activity"], "acceptable": [], "forbidden": ["call nondeterministic I O directly in replayed workflow code"] },
    { "id": "distributed-actor", "query": "address a stateful actor by id across cluster runners", "expected": ["cluster.entity"], "acceptable": [], "forbidden": ["assume exactly once message delivery"] },
    { "id": "event-history", "query": "append domain events and rebuild projections from an auditable history", "expected": ["eventlog.history"], "acceptable": [], "forbidden": ["use EventLog when only current state is needed"] },
    { "id": "reactive-graph", "query": "build derived frontend state with Effect services hydration and invalidation", "expected": ["atom.graph"], "acceptable": [], "forbidden": ["use a local AtomRef for an async service graph"] },
    { "id": "local-reactive", "query": "store high frequency local form state in a standalone reactive cell", "expected": ["atom.ref"], "acceptable": [], "forbidden": ["build a registry graph for an isolated hot cell"] },
    { "id": "typed-config", "query": "decode nested environment configuration and redact a secret", "expected": ["config.decode"], "acceptable": [], "forbidden": ["read process env throughout business logic"] },
    { "id": "production-telemetry", "query": "add structured logs traces metrics and graceful exporter flush", "expected": ["observability.telemetry"], "acceptable": [], "forbidden": ["log secrets or use logs as control flow"] },
    { "id": "virtual-time-test", "query": "test retries instantly with virtual time and a test Layer", "expected": ["testing.effect"], "acceptable": [], "forbidden": ["sleep in a unit test"] },
    { "id": "portable-llm", "query": "call a provider neutral language model with structured output", "expected": ["ai.language-model"], "acceptable": ["ai.toolkit"], "forbidden": ["parse unvalidated model JSON by hand"] },
    { "id": "tool-calling", "query": "let a language model invoke schema validated application tools", "expected": ["ai.toolkit"], "acceptable": ["ai.language-model"], "forbidden": ["execute arbitrary model text as code"] },
    { "id": "mcp-boundary", "query": "publish typed tools to external agents over MCP stdio or HTTP", "expected": ["ai.mcp"], "acceptable": ["ai.toolkit"], "forbidden": ["omit the supported MCP protocol list"] },
    { "id": "cache-choice", "query": "choose between caching a value refreshing a singleton and caching scoped resources", "match": "all", "expected": ["cache.lookup", "resource.refreshable", "scoped-cache.resource"], "acceptable": ["request-resolver.batch"], "forbidden": ["cache a resource without a finalizer"] },
    { "id": "durability-choice", "query": "choose current persisted state event history durable orchestration or distributed actors", "match": "all", "expected": ["persistence.store", "eventlog.history", "workflow.durable", "cluster.entity"], "acceptable": [], "forbidden": ["claim exactly once across external side effects"] }
  ]
}
